# Function to check if user input valid settings
checkInput <- function(X,y,est){
  # Check if X is null
  if(is.null(X)){
    stop("'X' is missing")
  }

  # Check if y is null
  if(is.null(y)){
    stop("'y' is missing")
  }

  # Check if there are NaN values in X

  # Check if there are NaN values in y

  # Check if S is a positive integer

  # Check if BatchSize is a positive integer

  # Check if Lrate is a positive numerical number

  # Check if MaxEpoch is positive integer

  # Check if LRateFactor is positive integer

  # Check if Patience is positive integer

  # Check if Network is a vector of integer

  # Check if Distribution is one of {"normal","binomial","poisson"}

  # Check if Seed is an integer

  # Check if Intercept is a logical number

  # Check if Momentum is a number from 0 to 1

  # Check if Verbose is positive integer

  # Check if WindowSize is positive integer


}
